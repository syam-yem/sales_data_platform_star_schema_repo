# This is a Databricks asset bundle definition for Sales_star_schema_bundles.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Sales_star_schema_bundles
  uuid: 62e21c1e-bfd6-46ed-9ff0-e70e54be4326



include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3406537184504964.4.azuredatabricks.net
      

  prod:
    mode: production
    workspace:
      host: https://adb-3406537184504964.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/syammgat2000@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/syammgat2000@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: syammgat2000@gmail.com
        level: CAN_MANAGE
